# Node / NestJS
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.env
.env.local
.env.*.local

# Bundled Flutter web copy used at runtime (built inside Docker)
backend/web/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors
.idea/
.vscode/
*.swp
.DS_Store

# OS
.DS_Store
Thumbs.db

# Local-only: extra CA certs injected at build time when behind a
# TLS-intercepting egress (corporate proxy). Not committed.
ca-certs/*.crt
docker-compose.override.yml
Dockerfile.local
